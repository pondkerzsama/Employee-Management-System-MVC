@model IEnumerable<EmployeeSystem.Models.Employee>

@{
    ViewData["Title"] = "รายชื่อพนักงาน";
}

<div class="container mt-4">
    <h2>📄 รายชื่อพนักงาน (Employee List)</h2>
    <hr />

    <a href="#" class="btn btn-primary mb-3">+ เพิ่มพนักงานใหม่</a>

    <table class="table table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>รหัส</th>
                <th>ชื่อ-นามสกุล</th>
                <th>เงินเดือน</th>
                <th>แผนก (ID)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.EmployeeID</td>
                    <td>@item.FirstName @item.LastName</td>
                    <td>@item.Salary.ToString("N0")</td> 
                    <td>@item.DeptID</td>
                </tr>
            }
        </tbody>
    </table>
</div>